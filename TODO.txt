TODO - Fonctionnalités à implémenter
=====================================

[ ] Réactiver les emails
    - Vérifier/déboguer la config SMTP existante
    - Tester l'envoi (assignation, rappels deadlines, certificats)

[~] Mécanisme de protection du contenu (licencing)
    [x] Phase 1 — Chiffrement contenu (commit cd6bbdf)
        - Paire de clés RSA-2048 par instance (générée auto, clé privée chiffrée avec AUTH_SECRET)
        - Cours H5P + documents GRC : chiffrement AES-256-GCM à l'upload, déchiffrement à la volée au serving
        - Vidéos natives : chiffrement AES-256-CTR (supporte Range requests / seeking)
        - Manifest signé RSA-PSS (non-répudiation : courseId, hash, auteur, date, instanceId)
        - Migration des contenus existants : panel dans Paramètres → "Protection du contenu"
        - Clé unique par instance → contenu illisible sur un autre système
    [x] Phase 2 — Licensing (commit 3ae8a33)
        - Token JWT signé par master keypair vendeur (RSA-PSS SHA256)
        - Modèles DB : InstanceConfig (clés RSA + licence + contentKey) + LicenseHistory
        - Double envelope : encryptedKey (RSA) + licenseEncryptedKey (contentKey) sur tout le contenu
        - Chaîne de clés pour renouvellements (previousWrappedKey dans LicenseHistory)
        - Page /activate accessible aux rôles admin sans sessionMode (anti chicken-and-egg)
        - Page /dashboard/admin/license : statut, renouvellement, historique, restauration clés
        - Recovery après réinstallation : POST /api/admin/license/recover-keys
        - CLI vendeur lms-license-cli/ : setup, issue, renew, list, info
        - Non-admins bloqués avec page "Application non activée" + bouton déconnexion
    [~] Tester le flow complet (voir TOTEST.txt) — Scénarios 1+2 validés, reste 3-7
    [ ] Alerte expiration licence
        - [x] Bannière in-app pour admin (< 30 jours) — lien vers /dashboard/admin/license
        - [x] Notification cloche admin avec icône ShieldAlert
        - [x] Entrée dans le journal d'audit (license.expiry_warning) — 1x/24h/admin
        - [ ] Email d'alerte admin quand < 30 jours (nécessite SMTP activé)

[ ] App Store / Marketplace de contenu
    - Catalogue de contenus disponibles à l'achat/déblocage
    - Contenu installable sur l'instance uniquement si licence valide
    - Lié au mécanisme de licencing (voir ci-dessus)
    - Gestion des contenus installés vs disponibles

[ ] Questions inline dans les cours PPTX (sous le coude, pas prioritaire)
    - Ajouter des questions à des positions de slides spécifiques
    - Interface d'édition dans le formulaire de cours
    - Injection dans le JSON H5P lors de la conversion

[ ] Parcours pédagogique (Learning Paths)
    - Enchaîner plusieurs cours dans un ordre défini
    - Débloquer le cours suivant uniquement à la complétion du précédent
    - Assignable comme un tout à un utilisateur / une équipe
    - Progression globale du parcours visible

[x] Tableau de bord reporting admin
    - Taux de complétion par cours, par équipe
    - Scores moyens aux quiz
    - Activité récente des apprenants (journal + export CSV)
    - Vue graphique (courbes activité 30j, bar charts complétion et scores)
    - Vignette "Actifs cette semaine" cliquable
    - Rapport d'activité sur 6 mois exportable CSV

[x] Export CSV
    - Export des progressions (avancement, statut, dates) par cours et par utilisateur
    - Export des résultats quiz (scores, seuils, réussite/échec, tentatives)
    - Admin : toutes les données / Manager : uniquement ses propres équipes
    - BOM UTF-8 pour compatibilité Excel

[x] Pagination des certificats
    - Vue apprenant : paginer la liste de ses certificats (client-side, PAGE_SIZE=15, tri date/titre)
    - Vue manager/créateur : idem via /dashboard/admin/certificates (scoped par équipes)

[x] Vue certificats admin
    - Visualiser les certificats de tous les apprenants
    - Groupé par utilisateur (vue "Par utilisateur") — avec nom de l'équipe affiché
    - Groupé par équipe (vue "Par équipe" — hiérarchie équipe > membre > certificats)
    - Avec pagination serveur (20 par page)

[x] Certificats Formation + GRC — couverture complète
    - Champ Type (Formation / Document GRC) dans l'onglet vérification
    - Template PDF adapté : "Attestation de lecture" vs "Certificat de réussite"
    - Export CSV : colonne Type, évaluation masquée pour GRC, ZIP inclut les deux types
    - Génération manuelle : cours ET documents GRC sélectionnables
    - Génération par équipe(s) : résolution automatique des membres, aperçu du total

[x] Confirmation du mot de passe lors du passage en mode admin
    - Saisie du mot de passe requise à chaque bascule vers le mode admin
    - Champ masqué / révélable, gestion erreur (401), soumission par Entrée
    - Exempt pour les comptes SSO (pas de passwordHash)

[x] Filtre "équipe du créateur" dans les bibliothèques
    - Bibliothèque de cours : sélection par équipe du créateur (toutes équipes, pas seulement la première)
    - Bibliothèque GRC : idem, filtre visible uniquement sur l'onglet Bibliothèque
    - Toolbar cours découpée en deux lignes (recherche non écrasée)
    - Admin : section "Bibliothèque" explicite avec compteur (cohérence avec vue manager)

[ ] Validité des certificats
    - Expiration d'un certificat après X mois configurables par cours
    - Relancer automatiquement la formation à l'expiration
    - Notification à l'apprenant et au manager

[ ] Note / feedback apprenant sur les cours
    - Activable/désactivable depuis les paramètres admin (désactivé par défaut)
    - L'apprenant peut noter le cours après complétion (1-5 étoiles)
    - Commentaire optionnel
    - Vue admin/manager des retours (note moyenne, distribution, liste commentaires)

[ ] Mettre en place HTTPS
    - Génération/configuration certificat TLS (Let's Encrypt ou self-signed)
    - Redirection HTTP → HTTPS
    - Mise à jour AUTH_URL dans .env

[ ] Dockeriser l'application
    - Dockerfile pour Next.js
    - docker-compose avec PostgreSQL, volumes pour uploads/backups
    - Variables d'environnement externalisées
    - Documentation de déploiement

[ ] Tester backup / restauration from scratch
    - Vérifier que le backup complet fonctionne (DB + fichiers)
    - Tester la restauration sur une instance vierge
    - Documenter la procédure

[ ] Résumé des notifications (email + cloche)
    - Faire un inventaire complet de tous les emails envoyés (triggers, templates, destinataires)
    - Faire un inventaire de toutes les notifications in-app (cloche en haut à droite)
    - Identifier les manques et incohérences

[ ] Intégration SIEM pour les logs
    - À discuter : choix du protocole (Syslog, CEF, JSON over HTTP...)
    - Envoyer les logs d'audit vers un SIEM externe
    - (Sujet à approfondir)

[x] Thèmes saisonniers / fêtes automatiques
    - Bannière festive automatique selon la date (Pâques calculé par algo Meeus, autres fixes)
    - Calendriers : France et Luxembourg (10 thèmes)
    - Activable/désactivable depuis les paramètres admin (désactivé par défaut)
    - Dismissible par l'utilisateur (mémorisé par thème)

[x] Bannière de maintenance
    - Configurable depuis les paramètres admin : toggle, message, type (warning/critique/info), date de fin
    - Visible sur toutes les pages pour tous les utilisateurs connectés
    - Disparaît automatiquement à la date de fin ou si désactivée
    - Prioritaire sur le thème saisonnier

[~] Module PDF / Attestation de lecture (GRC / Compliance)
    [x] Upload d'un ou plusieurs PDF (politiques, chartes, procédures...)
    [x] Assignables aux utilisateurs / équipes (modal assignation avec deadline commune)
    [x] Visionneuse PDF sécurisée (canvas PDF.js, pas de téléchargement direct)
    [x] Signature électronique horodatée (case "J'ai lu et j'accepte")
    [x] Attestation de lecture générée (template distinct "Attestation de lecture")
    [x] Historique signatures : admin voit tout, manager/créateur voient leur scope
    [x] Force-sign admin (marquer comme signé manuellement pour un utilisateur)
    [x] Lien direct vers attestation depuis la liste "Documents signés" (apprenant)
    [x] Dashboard admin : stats signatures (taux, nb assignés, nb signés)
    [x] Dashboard apprenant : bannière orange si documents en attente de signature
    [x] Scope manager/créateur : chacun voit uniquement son périmètre d'assignation

    [x] Activité PDF dans le journal d'audit admin (/dashboard/admin/activity)
        → document.signed / document.force-signed / document.upload remontent dans activité et reporting RH

    [x] Vérifier l'édition des documents (modal crayon dans la bibliothèque)
        → Modification titre, département (admin), remplacement PDF OK
        → Assignations/signatures préservées après remplacement

    [x] Améliorer le suivi des documents (signature)
        → Statut "En cours" remonte correctement
        → Cohérence admin/progress (onglet GRC) ↔ modal suivi signatures
        → document.view loggé à l'ouverture
        → Workflow complet validé : assigner → ouvrir → signer → voir attestation

    [x] Améliorer les vignettes PDF (performance + fraîcheur)
        → Vignette générée côté serveur à l'upload (script pdf-thumb.cjs, @napi-rs/canvas + pdfjs v4)
        → Stockée dans uploads/thumbnails/{docId}.jpg
        → Régénérée automatiquement si le fichier est remplacé via l'édition
        → Lazy generation au premier appel si absente (docs uploadés avant la feature)
        → Fallback icône FileText si génération impossible
        → Cache navigateur 24h (Cache-Control: private, max-age=86400)

    [x] Tester le workflow complet documents end-to-end
        → Uploader un vrai document PDF (ex: politique AUP, charte IT)
        → Tester département admin à l'upload + modification via édition
        → Assigner à des utilisateurs / équipes avec deadline
        → Se connecter en tant qu'apprenant : ouvrir, lire, signer
        → Vérifier l'attestation générée et accessible depuis "Documents signés"
        → Vérifier les stats admin (dashboard, progress, suivi signatures)
        → Vérifier que le manager voit bien son scope uniquement
        → Vérifier l'export CSV documents-progress (endpoint à implémenter si nécessaire)
